name: build
on:
  push:
  pull_request:
    branches: master
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: '12.x'
      - uses: subosito/flutter-action@v1
        with:
          channel: 'stable' # or: 'beta', 'dev' or 'master'
      - run: flutter pub get
      # #IOS
      - run: |
          cd example
          cd ios ruby <(curl https://connect.squareup.com/readersdk-installer) install --app-id ${{secrets.SQUARE_READER_SDK_APPLICATION_ID}} --repo-password ${{secrets.SQUARE_READER_SDK_REPOSITORY_PASSWORD}}>
      # #IOS
      #   - run: |
      #     cd example
      # Android
      # - run: |
      #     cd example
      #     flutter pub get
      #     cd android
      #     ./gradlew clean build -PSQUARE_READER_SDK_APPLICATION_ID=${{secrets.SQUARE_READER_SDK_APPLICATION_ID}} -PSQUARE_READER_SDK_REPOSITORY_PASSWORD=${{secrets.SQUARE_READER_SDK_REPOSITORY_PASSWORD}}
      


    # name: build
# on: [push]
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - uses: actions/setup-java@v1
#         with:
#           java-version: "12.x"
#       - uses: subosito/flutter-action@v1
#         with:
#           channel: "stable" # or: 'beta', 'dev' or 'master'
#       - run: flutter pub get
#   # build-android:
#   #   runs-on: ubuntu-latest
#   #   steps:
#   #     - name: Checkout the current repo
#   #       uses: actions/checkout@v3
#       #IOS
#       # - run: |
#       #     cd example
#       #     cd ios
#       #     ruby <(curl https://connect.squareup.com/readersdk-installer) install --app-id ${{ secrets.USERNAME }} --repo-password ${{ secrets.PASSWORD }}>
#       #     cd ..
#       #     flutter pub get
#       #     flutter build ios --no-codesign
#       #Android
#       # - run: |
#       #     cd example
#       #     flutter pub get
#       #     openssl aes-256-cbc -K ${{secrets.ENCRYPTED_KEY}} -iv ${{secrets.ENCRYPTED_IV}}
#       #       - in android/gradle.properties.enc -out android/gradle.properties -d
#       #     flutter build apk
